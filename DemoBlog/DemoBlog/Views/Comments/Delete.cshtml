@model DemoBlog.Models.Comment

@{
    ViewBag.Title = "Delete Comment";
    var toPost = Model.PostComm.Id;
}

<div>
    <h2>Delete Comment</h2>
</div>

<hr />

<h3>Are you sure you want to delete this?</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Body)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Body)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Date)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Date)
        </dd>

    </dl>

    <hr />

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default btn-sm" />
            @Html.ActionLink("Back to List", "Details", "Posts", new { id = toPost }, new { @class = "btn btn-default btn-sm" })
        </div>
    }
</div>
